import { expect } from 'chai';
import pkg from 'hardhat';
const { ethers } = pkg;

describe('Basic Contract', function () {
  it('should deploy', async function () {
    // Replace with actual contract deployment test
    expect(true).to.equal(true);
  });
});
