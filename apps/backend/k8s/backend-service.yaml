apiVersion: v1
kind: Service
metadata:
  name: finainexus-backend-service
  namespace: finainexus
spec:
  selector:
    app: finainexus-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
